# 03 — Create CONTEXT.md and add to .gitignore

**What to build:** `CONTEXT.md` exists at repo root (initially empty) and is gitignored. This satisfies the domain-modeling skill requirement without polluting the repo.

**Blocked by:** None — can start immediately

**Status:** done

- [ ] `CONTEXT.md` is created at repo root (empty or with minimal placeholder)
- [ ] `CONTEXT.md` is added to `.gitignore`
