# 12 — Replace img with next/image (@next/next/no-img-element)

**What to build:** Replace all `<img>` elements with Next.js `<Image>` component for optimized image loading.

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] All `<img>` elements replaced with `<Image>`
- [ ] Image dimensions (width/height) properly specified
- [ ] `tsc --noEmit` passes
- [ ] `pnpm lint` passes (no @next/next/no-img-element warnings)
