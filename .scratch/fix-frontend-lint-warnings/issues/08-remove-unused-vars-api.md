# 08 — Remove unused vars in src/lib/api/

**What to build:** Delete all unused imports and variables in API client files. No behavioral change, zero risk.

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] All unused imports removed from API clients
- [ ] All unused variables removed from API clients
- [ ] `tsc --noEmit` passes
- [ ] `pnpm lint` passes (warning count decreases)
