# 06 — Remove unused vars in src/app/(dashboard)/remaining modules/

**What to build:** Delete all unused imports and variables in remaining dashboard pages (quality, registration, research, production, etc.). No behavioral change, zero risk.

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] All unused imports removed from remaining dashboard pages
- [ ] All unused variables removed from remaining dashboard pages
- [ ] `tsc --noEmit` passes
- [ ] `pnpm lint` passes (warning count decreases)
