# 09 — Remove unused vars in src/actions/

**What to build:** Delete all unused imports and variables in action files. No behavioral change, zero risk.

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] All unused imports removed from actions
- [ ] All unused variables removed from actions
- [ ] `tsc --noEmit` passes
- [ ] `pnpm lint` passes (warning count decreases)
