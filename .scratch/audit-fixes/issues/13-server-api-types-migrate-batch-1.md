# 13: Server API types - migrate batch 1 (CRUD operations)

**What to build:** Create/update/delete operations use proper types. This is the second step of an expand-contract pattern. CRUD operations in server API files should be migrated to use proper types instead of `data: any`.

**Blocked by:** 12

**Status:** ready-for-agent

- [ ] Create operations use proper types instead of `data: any`
- [ ] Update operations use proper types instead of `data: any`
- [ ] Delete operations use proper types instead of `data: any`
- [ ] Generated types from schema.ts used for request bodies
- [ ] No functionality broken
- [ ] TypeScript compilation succeeds
- [ ] Tests still pass
