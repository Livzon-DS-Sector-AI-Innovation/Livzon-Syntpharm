# 02 — 创建轻量级位置编辑器 (LocationEditor)

**What to build:** 参照 `CategoryEditor` 的模式，创建一个专用的 `LocationEditor` 组件。

**Blocked by:** 01

**Status:** ready-for-agent

## Acceptance Criteria
- [ ] 创建 `frontend/src/components/equipment/LocationEditor.tsx`
- [ ] 支持“新增根位置”和“新增子位置”两种模式
- [ ] 集成现有的 `createLocation` Action
