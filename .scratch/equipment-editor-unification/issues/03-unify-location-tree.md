# 03 — 统一 LocationTree 交互逻辑

**What to build:** 修改 `LocationTree.tsx`，接入新创建的 `LocationEditor`。

**Blocked by:** 02

**Status:** ready-for-agent

## Acceptance Criteria
- [ ] 在 `LocationTree` 顶部添加与 CategoryTree 一致的“新建位置”按钮
- [ ] 空状态下显示带 CTA 按钮的引导卡片
- [ ] 删除 `LocationTree` 中对 `openLocationDrawer` 的调用
